using UnityEngine;
using System.Collections;

public class Levels 
{
	// IMPORTANT: Enum names should be the same as the scene names
	public enum Enum 
	{ 
		// Must start with zero
		Temple = 0,
		COUNT
	};
}
